name: Dynamic Analysis (Fuzzing)

on:
  pull_request:
    branches: ["main"]
  schedule:
    - cron: '0 0 * * 0'   # Weekly, Sunday at midnight UTC

permissions:
  contents: read

jobs:
  fuzz:
    name: Fuzz test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # master
        with:
          toolchain: stable

      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2

      # Scheduled runs get a much larger budget than pull request runs, so review
      # stays fast while the weekly job explores the input space deeply.
      - name: Install GTK development packages
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends libgtk-4-dev libadwaita-1-dev

      - name: Run property-based tests
        env:
          PROPTEST_CASES: ${{ github.event_name == 'schedule' && 100000 || 1024 }}
        run: make fuzz

  cargo-fuzz:
    name: cargo-fuzz (parser)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # master
        with:
          toolchain: nightly
      - name: Install cargo-fuzz
        run: cargo install cargo-fuzz --locked
      # docs/architecture.md §13.2: zero panics over 10^6 inputs. A short run
      # on pull requests, the full million on the weekly schedule.
      - name: Fuzz parse_rule and round_trip
        env:
          RUNS: ${{ github.event_name == 'schedule' && 1000000 || 100000 }}
        run: make fuzz-parser FUZZ_RUNS="$RUNS"

  gate:
    name: Dynamic analysis gate
    runs-on: ubuntu-latest
    needs: [fuzz, cargo-fuzz]
    if: always()
    steps:
      - name: Check fuzzing result
        env:
          RESULT: ${{ needs.fuzz.result == 'success' && needs.cargo-fuzz.result || needs.fuzz.result }}
        run: |
          if [ "$RESULT" != "success" ]; then
            echo "::error::Fuzzing failed. See DYNAMIC_ANALYSIS_POLICY.md for the triage process."
            exit 1
          fi
          echo "Dynamic analysis passed."
